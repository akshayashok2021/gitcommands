### Creating your repository

```
git status 
git init  
git status
git commit -m "Added workshop text file"
git config --list
git config user.name "replace_with_your_github_username"
git config user.email "replace_with_your_github_email"
git config --list
git status
git commit -m "Added workshop text file"
git status
git branch -M main
git status
git push 
git remote add origin https://github.com/akshayashok2021/test.git
git config --list

```
